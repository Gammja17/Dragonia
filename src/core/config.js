// 게임 전역 상수. 지도별 좌표는 data/maps.js 로 옮겼다.

export const DAY_LENGTH = 240; // 하루 길이(초)

export const MAX_KIDS = 20;   // 이름 풀(data/npcs.js 의 KID_NAMES)이 24개라 여기까지는 이름이 겹치지 않는다
export const MAX_PARTICLES = 350;
export const MAX_BULLETS = 100;
export const MAX_ENEMIES = 14;   // 한 지도에 유지되는 적 수 (지도가 작아져서 줄였다)

export const RAID_FIRST_DELAY = 120; // 초
export const RAID_INTERVAL = 150;
